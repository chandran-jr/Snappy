#include <stdio.h>

int main () {
	
    // Isi i dengan nilai 0
	int i = 0;
	
    // Lakukan perintah
	do
	{
		// Cetak nilai i
		printf("%d\n", i);
		i++;
	}
	while (i < 0); // Selama i kurang dari 0
}