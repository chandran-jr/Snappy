import React from 'react';

const videoConstraints = {
    width: 250,
    height: 400,
    facingMode: 'user',
};

function WebcamCapture() {
    return (
        <div>
            
        </div>
    )
}

export default WebcamCapture
